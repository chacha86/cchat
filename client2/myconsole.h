#pragma once
#include <stdio.h>
#include <windows.h>
#include <conio.h>  // kbhit와 getch 함수를 사용하기 위해 필요

char* run_my_console();
char* get_msg();
int get_tmp_msg_size();
void setx(int x);
void sety(int y);
